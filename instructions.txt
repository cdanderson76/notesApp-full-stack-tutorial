DATE AND CREATE


NOTE: .env FILE INFORMATION HAS TO BE PROVIDED FOR THIS CHALLENGE!


1. Create '/lib/utils.js' and create a utility function that will format the date
   to make it more readable.  Once created, take this over to 'NoteCard.jsx' and
   use it to update the format.


2. Add a 'div' to 'App.jsx' to enhance the background.  Use the following class to
   do so:

   className="absolute inset-0 -z-10 h-full w-full items-center px-5 py-24
              [background:radial-gradient(125%_125%_at_50%_10%,#000_60%,#00FF9D40_100%)]"

   Also, for the parent 'div' in 'App.jsx', set up the tailwind utility classes to be
   'relative', and have a 'full width and height' viewport'.  REMOVE the 'forest' theme.


3. Go to 'CreatePage.jsx' and set up state for the 'title' and the 'content' (we are
   doing this because both will have these fields in order to create notes).

   Also set up a 'loading' state for when the page loads.

   Create a function that is going to handle the submissions of data.


4. Use the following boilerplate code to set up 'CreatePage.jsx'. Ensure that you provide
   a 'Link' tag that will get you back to 'HomePage.jsx':

   Link tag: className="btn btn-ghost mb-6"

   <div className="min-h-screen bg-base-200">
      <div className="container mx-auto px-4 py-8">
        <div className="max-w-2xl mx-auto">
          { LINK TAG AND ICON GOES HERE }
          <div className="card bg-base-100">
          <div className='card-body'>
            <h2 className="card-title text-2xl mb-4"></h2>
            <form>
              <div className="form-control mb-4">
              <label className="label">
                <span className="label-text"></span>
              </label>
              <input type="text" 
                       placeholder=""
                       className="input input-bordered" />
              </div>
              <div className="form-control mb-4">
              <label className="label">
                <span className="label-text"></span>
              </label>
              <textarea
                        placeholder=""
                        className="textarea textarea-bordered h-32" />
              </div>
              <div className="card-actions justify-end">
                <button className="btn btn-primary"
                        type='submit'></button>
              </div>
            </form>
          </div>
          </div>
        </div>
      </div> 
    </div>


   Add the 'ArrowLeftIcon' from lucide react, with the text of 'Back to Notes'.  This
   should take you back to 'HomePage.jsx'.  Use the following for this icon:

   ArrowLeftIcon: className='size-5'



Solution: https://shorturl.at/pZhcl (2:28:10 - 2:37:36)
